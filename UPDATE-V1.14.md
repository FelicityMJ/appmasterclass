# Update to DataApp Studio V1.14

V1.14 cleans up transparent database Lists.

- Transparent means no list box border and no row divider lines.
- Database-list thumbnail photos now fill the entire circular crop using `object-fit: cover`.
- Fixes inherited padding that made list photos look like a smaller image inside a larger circle in Design/Test.
- Behaviour matches in Design, Test and the published Android/PWA app.
- No Firebase rule changes are required.
