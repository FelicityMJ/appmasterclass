# Update to DataApp Studio V1.15

V1.15 separates database text into **Short text** and **Long text**. Existing `text` fields are migrated automatically to Short text. Long text is edited in a multi-line database cell and displays inside the Scrollable text box component with wrapping and vertical scrolling in Design, Test and published apps.

No Firebase rule changes are required.
